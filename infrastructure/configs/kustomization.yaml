---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Cluster-wide configs (e.g. cert-manager ClusterIssuers, network policies).
# Empty for now — add resources here as the cluster grows.
resources: []
